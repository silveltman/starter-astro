{
  "ssg": "astro",
  "build_configuration": {
    "preserved_paths": "",
    "environment_variables": [
      { "key": "EDITING", "value": "FALSE" },
      { "key": "PRODUCTION", "value": "true" }
    ],
    "install_command": "yarn install",
    "build_command": "yarn build",
    "output_path": "dist"
  }
}
